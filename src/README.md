The main source is currently split into 2 projects:

* main: contains the base source code for a distributed P2P dictionary
* persistence: an additional module for serailizing a dictionary into XML format
